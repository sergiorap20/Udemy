import { ConvertirArrayPipe } from './convertir-array.pipe';

describe('ConvertirArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
